apiVersion: v1
kind: Service
metadata:
  name: tier-to-group-admin
  namespace: maas-dev
  labels:
    app: tier-to-group-admin
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: tier-to-group-admin

